const KeySkills = () => {
	return (
		<div>
			<h2>Mes Compétences</h2>
			<ul>
				{/* {skillsData.map((skill, index) => (
          <li key={index}>
            {skill.name} - {skill.level}
          </li>
        ))} */}
			</ul>
		</div>
	);
};

export default KeySkills;
